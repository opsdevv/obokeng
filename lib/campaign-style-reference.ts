/**
 * Style and structural reference for generated campaigns (matches portfolio brief format).
 */
export const CAMPAIGN_STYLE_REFERENCE = `
REFERENCE STRUCTURE AND TONE (mirror this depth and layout, adapted to the user's brief):

1) Campaign title + short hook line (like "Built different" — punchy, not generic).

2) Opening narrative (2–4 short paragraphs): emotional, specific audience, why it matters beyond the product/event.

3) "This campaign is not about X; it's about Y" pivot.

4) Campaign pillars (4 items): each a bold principle + one clarifying line.

5) Campaign Framework:
   - Campaign Theme (ties abstract to concrete proof)
   - Campaign mantra (repeatable line)

6) Campaign Phases & Key Assets — for EACH phase include:
   - Phase name
   - Goal
   - Copy Approach (voice note)
   - Sample copy blocks where useful (headlines, body, hashtags, CTA)
   - Key assets (expo, signage, email subjects, UGC angles) when relevant

7) Closing rally lines + CTA.

Use direct, confident copy; avoid corporate filler. Short lines and rhythm beats long paragraphs.
`.trim()
